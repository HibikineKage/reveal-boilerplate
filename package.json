{
  "name": "reveal-boilerplate",
  "version": "0.1.0",
  "description": "Reveal.js Boilerplate",
  "main": "index.html",
  "repository": "https://github.com/HibikineKage/reveal-boilerplate",
  "author": "Hibikine Kage",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url /reveal-boilerplate",
    "deploy": "yarn build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.7.1"
  },
  "dependencies": {
    "reveal": "^0.0.4"
  }
}
